{
  "mcpServers": {
    "massive": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/massive-com/mcp_massive@v0.9.0",
        "mcp_massive"
      ],
      "env": {
        "MASSIVE_API_KEY": "${MASSIVE_API_KEY}"
      }
    }
  }
}
